<div class="container-fluid px-5 py-5">
  <div class="mb-5">
    <h1 class="display-4 fw-bold mb-2" style="color: #070485;">Les destinations</h1>
    <h2 class="h4 fw-normal" style="color: #019dbd;">Explorez les 6 continents en toute liberté</h2>
  </div>

  <div class="row g-4">
    <% @continents.each do |continent| %>
      <div class="col-12 col-md-6 col-lg-4">
        <div class="card border-0 shadow-sm bg-white overflow-hidden h-100">

          <div class="ratio ratio-16x9 bg-light">
            <% if continent.banner_url.present? %>
              <img src="<%= continent.banner_url %>"
                   class="card-img-top object-fit-cover"
                   alt="<%= continent.name %>">
            <% else %>
              <div class="d-flex align-items-center justify-content-center h-100">
                <i class="fa-solid fa-earth-americas text-secondary fs-1"></i>
              </div>
            <% end %>
          </div>

          <div class="card-body d-flex flex-column p-3">
            <h5 class="card-title fw-bold m-0" style="color: #070485;"><%= continent.name %></h5>

            <div class="mt-auto pt-3 d-flex justify-content-end">
              <% if continent.name == "Moyen-Orient" %>
                <%= link_to "Sélectionner", destinations_path(continent: continent.name),
                            class: "btn px-4 fw-bold shadow-sm text-white",
                            style: "background-color: #d35400; border: none; border-radius: 5px;" %>
              <% else %>
                <span class="btn px-4 fw-bold shadow-sm text-white"
                      style="background-color: #d35400; border: none; border-radius: 5px;">
                  Sélectionner
                </span>
              <% end %>
            </div>
          </div>

        </div>
      </div>
    <% end %>
  </div>
</div>
